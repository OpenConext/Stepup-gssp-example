framework:
    test: ~
    session:
        storage_factory_id: session.storage.factory.mock_file
    profiler:
        collect: false
    http_client:
        default_options:
            verify_host: false
            verify_peer: false
